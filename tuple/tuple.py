coordinate = (4, 5)
print(coordinate)
print(coordinate[0])
print(coordinate[1])
# Tuples are immutable, so you cannot change their values
# coordinate[0] = 10  # This will raise an error
# Tuples can contain different data types
person = ("Snehal", 25, 5.5, True)
print(person)
# You can also create a tuple with a single element, but you need to add a comma    
single_element_tuple = (42,)